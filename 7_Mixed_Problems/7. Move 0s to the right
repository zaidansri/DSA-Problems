void pushZerosAtEnd(vector<int> &arr) 
{
	// Write your code here.
	int n = arr.size();
	int ans[n] = {0};
	int j = 0;

	for (int i = 0; i<n ; i++) {
		if (arr[i] != 0){
			ans[j] = arr[i];
			j++;
		}
	}
	//std :: copy (ans, ans + n, arr);
	for (int i = 0; i<n ; i++) {
		arr[i] = ans[i];
	}
}
